
> ⚠️ **Important:**  
> The `rbe-client/` folder **must** be included; it contains the RBE client binaries that will be installed to `/opt/reclient`.

---

## 🛠 Requirements

- Ubuntu 22.04 or 24.04  
- sudo privileges  
- BuildBuddy API key (included inside the script before exicuting)  
- At least 4GB RAM (for reproxy)

---

## 📥 Installation

Clone the repo:

```bash
git clone https://github.com/Sanjis-Android-Playground/rbe.git
cd rbe
chmod +x install_rbe.sh
./install_rbe.sh
